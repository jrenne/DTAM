% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PreferredHabitat.R
\name{solve_PH_TSM}
\alias{solve_PH_TSM}
\title{Solve a preferred-habitat term-structure model}
\usage{
solve_PH_TSM(model, max_iter = 100, zeta_bar = 3)
}
\arguments{
\item{model}{A list describing the model. For the single-curve case it must
contain at least \code{gamma}, \code{mu}, \code{Phi}, \code{Sigma}, \code{alpha}, \code{beta}, \code{xi},
\code{a1}, and \code{b1}. Additional fields such as \code{alpha_star}, \code{beta_star},
\code{xi_star}, \code{mu_e0}, \code{mu_e1}, \code{a1_star}, \code{b1_star}, \code{omega}, \code{mu_K0},
\code{mu_K1}, \code{alpha_K}, \code{beta_K}, and \code{xi_K} activate the two-curve and stock
variants.}

\item{max_iter}{Number of fixed-point iterations used to solve the model.}

\item{zeta_bar}{Initial guess for the average log price-dividend ratio in the
stock/perpetuity case.}
}
\value{
A list containing the solved affine bond-pricing coefficients and
related equilibrium objects. Core outputs include:
\code{A}, \code{B}, \code{a}, \code{b}, \code{alpha}, \code{beta}, \code{lambda0}, \code{lambda1}, \code{muQ}, and
\code{PhiQ}. Additional objects such as \code{A_star}, \code{a_star}, \code{mu_e0}, \code{mu_e1},
\code{mu_zeta0}, \code{mu_zeta1}, \code{C_K}, \code{D_K}, and \code{Theta_K} are returned when the
corresponding model extensions are active.
}
\description{
Solves the preferred-habitat term-structure model under one of three
configurations: a single yield curve, two interacting yield curves, or a
single yield curve with stock/perpetuity pricing.
}
\details{
The model combines an affine short-rate specification with maturity-specific
net supply effects borne by risk-averse arbitrageurs. The function iterates
on bond-loadings, risk prices, and (when relevant) auxiliary two-curve or
stock-pricing objects until convergence.

In the single-curve case, the output \code{a} and \code{b} provide the yield
coefficients:

\deqn{
y_{t,h} = b_h + a_h^\prime w_t.
}{
y_{t,h} = b_h + a_h' w_t.
}
}
\examples{
# Example adapted from the preferred-habitat chapter of the companion
# Bookdown project.
H <- 12
a <- 20
n <- 2
mu <- matrix(c(0.03, 0), n, 1)
Phi <- matrix(c(0.95, 0,
                0,    0.90), n, n, byrow = TRUE)
Sigma <- diag(c(1e-4, 2e-4))
a1 <- matrix(c(1, 0), n, 1)
b1 <- 0

Alpha <- cbind(
  0.02 * exp(-0.15 * (1:H)),
  0.01 * exp(-0.10 * (1:H))
)
Beta <- 0.6 * exp(-0.08 * (1:H))
xi <- 0.02 * (1:H) * exp(-0.08 * (1:H))

model_VV <- list(
  gamma = a,
  mu = mu,
  Phi = Phi,
  Sigma = Sigma,
  alpha = Alpha,
  beta = Beta,
  xi = xi,
  a1 = a1,
  b1 = b1
)

RES <- solve_PH_TSM(model_VV, max_iter = 100)

dim(RES$a)
RES$beta[1]

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
