% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/procedures.R
\name{price_Stock_calls_puts}
\alias{price_Stock_calls_puts}
\title{Price stock options from affine state dynamics}
\usage{
price_Stock_calls_puts(
  W,
  S,
  H,
  a,
  b,
  K_over_S,
  psi,
  parameterization,
  max_x = 1e+05,
  dx_statio = 10,
  min_dx = 1e-05,
  nb_x1 = 5000
)
}
\arguments{
\item{W}{Matrix of state-vector values.}

\item{S}{Vector of ex-dividend stock prices.}

\item{H}{Maximum maturity in model periods.}

\item{a, b}{Stock-return loadings.}

\item{K_over_S}{Vector of strikes expressed as fractions of the spot price.}

\item{psi}{One-step Laplace transform of the state vector.}

\item{parameterization}{List containing at least the pricing model and the
short-rate specification.}

\item{max_x, dx_statio, min_dx, nb_x1}{Numerical integration settings.}
}
\value{
A list with arrays \code{Calls} and \code{Puts}.
}
\description{
Prices European stock calls and puts when log returns are affine in the state
vector and the payoff is computed through truncated Fourier inversion.
}
\examples{
model <- list(n_w = 1, mu = matrix(0, 1, 1), Phi = matrix(0.9, 1, 1),
              Sigma12 = matrix(0.1, 1, 1))
W <- matrix(c(0, 0.1), 2, 1)
S <- c(100, 102)
res <- price_Stock_calls_puts(
  W = W, S = S, H = 2, a = 0.1, b = 0,
  K_over_S = c(0.95, 1.05),
  psi = psi.GaussianVAR,
  parameterization = list(model = model, xi0 = 0, xi1 = 0)
)
dim(res$Calls)

}
