% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/affine-bond-pricing.R
\name{compute_AB_thk}
\alias{compute_AB_thk}
\title{Compute affine coefficients for delayed-horizon payoffs}
\usage{
compute_AB_thk(xi0, xi1, u, H, k, psi, psi.parameterization, u2 = NaN, x = 0)
}
\arguments{
\item{xi0}{Scalar intercept in the short-rate equation.}

\item{xi1}{Loading vector in the short-rate equation.}

\item{u}{Loading matrix applied to the delayed-horizon state.}

\item{H}{Maximum maturity, in model periods.}

\item{k}{Indexation lag of the payoff, in model periods.}

\item{psi}{Conditional Laplace transform of the state vector.}

\item{psi.parameterization}{List of parameters passed to \code{psi}.}

\item{u2}{Optional loading matrix applied to the cumulative future state
terms. If left as \code{NaN}, the function sets \code{u2 = 0}.}

\item{x}{Scalar multiplier applied to the intermediate-state sum.}
}
\value{
A list with arrays \code{A} and \code{B}, the affine coefficients of the
date-\code{t} price.
}
\description{
Computes affine pricing coefficients for payoffs involving the state vector
at a delayed horizon and, optionally, sums of intermediate future states.
}
\details{
The function is used for claims of the form
\deqn{
\exp\left(
  u^\prime \left(w_{t+h-k} + x[w_{t+h-k-1} + \cdots + w_{t+1}]\right)
  + u_2^\prime (w_{t+h} + \cdots + w_{t+1})
\right),
}{
exp(u' (w_{t+h-k} + x[w_{t+h-k-1} + ... + w_{t+1}]) +
  u2' (w_{t+h} + ... + w_{t+1})).
}

The function returns \code{NaN} coefficients for horizons \code{h < k}, since the
delayed payoff is not yet defined at those maturities.

This helper is used in option-pricing and indexed-payoff applications in the
Bookdown companion project.
}
\examples{
model <- list(
  mu = matrix(c(0.01, 0.02), ncol = 1),
  Phi = matrix(c(0.95, 0.00,
                 0.05, 0.90), nrow = 2, byrow = TRUE),
  Sigma = diag(c(0.02, 0.01))
)

xi0 <- 0
xi1 <- matrix(c(1, 0), ncol = 1)
u <- matrix(c(0, 1), ncol = 1)

res <- compute_AB_thk(
  xi0 = xi0,
  xi1 = xi1,
  u = u,
  H = 6,
  k = 2,
  psi = psi.GaussianVAR,
  psi.parameterization = model
)

dim(res$A)
dim(res$B)

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
