% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shadow-rate.R
\name{compute_F_Shadow_affine}
\alias{compute_F_Shadow_affine}
\title{Affine approximation for shadow-rate forward-rate components}
\usage{
compute_F_Shadow_affine(
  W,
  psi,
  psi.parameterization,
  ell_bar = 0,
  b,
  a,
  c,
  chi = -1,
  H,
  eps = 10^(-6),
  max_x = 2000,
  dx_statio = 10,
  min_dx = 1e-06,
  nb_x1 = 1000,
  du = 1e-06
)
}
\arguments{
\item{W}{Matrix of state values, with one row per date.}

\item{psi}{Conditional Laplace transform of the state vector.}

\item{psi.parameterization}{Parameter list passed to \code{psi}.}

\item{ell_bar}{Lower bound applied to the short rate.}

\item{b}{Scalar intercept in the short-rate specification.}

\item{a}{Loading vector in the short-rate specification.}

\item{c}{Loading vector entering the affine approximation formula.}

\item{chi}{Sign/convention parameter used in the approximation formula.}

\item{H}{Maximum horizon, in model periods.}

\item{eps}{Small finite-difference increment used to compute the derivative
term \code{dG}.}

\item{max_x}{Maximum truncation point of the numerical integration grid.}

\item{dx_statio}{Constant step size used on the upper tail of the integration
grid.}

\item{min_dx}{Smallest step size used at the beginning of the grid.}

\item{nb_x1}{Number of log-spaced grid points used before switching to
\code{dx_statio}.}

\item{du}{Finite-difference step used to compute conditional variance terms.}
}
\value{
A list with two matrices, \code{F_c_equal_0} and \code{F}, each of dimension
\verb{T x H}, where \code{T} is the number of rows of \code{W}.
}
\description{
Computes the affine approximation of the sequence of forward-rate correction
terms that arise in shadow-rate models.
}
\details{
The function combines:
\code{truncated.payoff()} to evaluate the truncated exponential component,
\code{compute_expect_variance()} and \code{compute_expect_variance_H()} to recover the
conditional mean and variance terms entering the affine approximation.

This routine is used in the nonlinear/shadow-rate chapter of the companion
Bookdown project.
}
\examples{
rho <- 0.96
Phi <- matrix(rho, 1, 1)
mu <- matrix(0.03 * (1 - rho), 1, 1)
Sigma <- (1 - rho^2) * 0.02^2 * matrix(1)
model <- list(mu = mu, Phi = Phi, Sigma = Sigma, n_w = 1)

set.seed(123)
W <- simul.GVAR(model, nb.sim = 40)

res <- compute_F_Shadow_affine(
  W = W,
  psi = psi.GaussianVAR,
  psi.parameterization = model,
  ell_bar = 0,
  b = 0,
  a = matrix(1, 1, 1),
  c = matrix(0, 1, 1),
  H = 6,
  max_x = 300,
  dx_statio = 2,
  min_dx = 1e-4,
  nb_x1 = 200
)

dim(res$F)

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
