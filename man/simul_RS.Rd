% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proc_KitagawaHamilton.R
\name{simul_RS}
\alias{simul_RS}
\alias{make_Pi_z_kron_z}
\alias{compute_LT_RS}
\title{Regime-switching simulation and transition-matrix utilities}
\usage{
simul_RS(Omega, TT, ini_state = NaN)

make_Pi_z_kron_z(Pi)

compute_LT_RS(alpha, Pi, Maturities_decompo, indic_add_current = FALSE)
}
\arguments{
\item{Omega, Pi}{Transition matrix with rows summing to one. \code{Pi} can also be
a three-dimensional array when the transition matrix varies
deterministically over time.}

\item{TT}{Number of simulated dates.}

\item{ini_state}{Optional initial regime. If omitted, the simulation starts
from the most likely state under the stationary distribution.}

\item{alpha}{Loading vector entering the Markov-switching Laplace transform.}

\item{Maturities_decompo}{Integer decomposition matrix describing how target
maturities are assembled from previously computed shorter maturities.}

\item{indic_add_current}{Logical indicating whether the current state
contribution should be included in the Laplace transform coefficients.}
}
\value{
\code{simul_RS()} returns a \verb{TT x J} indicator matrix of simulated regimes.
\code{make_Pi_z_kron_z()} returns the transition matrix (or array) for the
augmented chain. \code{compute_LT_RS()} returns a list with \code{A} (the transform
coefficients) and \code{H} (the associated maturities).
}
\description{
\code{simul_RS()} simulates a finite-state Markov chain. \code{make_Pi_z_kron_z()}
builds the transition matrix associated with the augmented chain
\verb{z_t \\\%x\\\% z_\{t-1\}}. \code{compute_LT_RS()} computes multi-horizon Laplace
transforms for payoffs driven by a Markov-switching state.
}
\examples{
Pi <- matrix(c(0.9, 0.1,
               0.2, 0.8), 2, 2, byrow = TRUE)

z <- simul_RS(Pi, TT = 10)
dim(z)

PI_aug <- make_Pi_z_kron_z(Pi)
dim(PI_aug)

decomp <- matrix(c(2, 0,
                   1, 2), 2, 2, byrow = TRUE)
res <- compute_LT_RS(alpha = matrix(c(-0.1, 0.2), 2, 1),
                     Pi = Pi,
                     Maturities_decompo = decomp)
res$H

}
