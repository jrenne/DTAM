% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proc_learning.R
\name{solve_Learning_RE}
\alias{solve_Learning_RE}
\title{Solve a learning model with rational-expectations feedback}
\usage{
solve_Learning_RE(model, max.iter = 200)
}
\arguments{
\item{model}{A list containing the model matrices:
\code{mu}, \code{Phi}, \code{Sigma12}, \code{Omega12}, \code{A}, \code{B}, \code{Lambda0}, \code{Lambda1},
\code{Psi0}, \code{Psi1}, \code{Gamma0}, and \code{Gamma1}.}

\item{max.iter}{Maximum number of iterations used in the fixed-point loops.}
}
\value{
A list containing the solved model objects, including \code{R}, \code{P}, \code{K},
\code{S}, \code{mu_ww}, \code{Phi_ww}, \code{Sigma_ww}, and \code{Sigma12_ww}.
}
\description{
Solves an extended learning model in which the state dynamics depend on
filtered beliefs and on forward-looking expectations under rational
expectations.
}
\details{
The model is of the form
\deqn{
w_t = \mu + \Phi w_{t-1}
      + \Lambda_0 w_{t|t} + \Lambda_1 w_{t-1|t-1}
      + \Psi_0 E(w_{t+1}|w_t) + \Psi_1 E(w_t|w_{t-1})
      + \Gamma_0 E(w_{t+1}|y_t) + \Gamma_1 E(w_t|y_{t-1})
      + \Sigma^{1/2}\varepsilon_t.
}

The function first solves for the forward-looking matrix \code{Phi_1}, then for
the feedback matrix \code{Phi_2}, then computes the steady-state filtering
matrices, and finally constructs the joint law of \verb{(w_t', w_\{t|t\}')'}.

A warning is issued if the resulting transition matrix is not stationary.
}
\examples{
# This function requires a fully specified model list.
# See the package vignettes or examples in the source code.

}
