% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proc_kalman.R
\name{Kalman_filter}
\alias{Kalman_filter}
\title{Kalman filter for linear Gaussian state-space models}
\usage{
Kalman_filter(
  Y_t,
  nu_t,
  H,
  N,
  mu_t,
  G,
  M,
  Sigma_0,
  rho_0,
  indic_pos = 0,
  Rfunction = Rf,
  Qfunction = Qf,
  reconciliationf = function(x, opt) {
     x
 }
)
}
\arguments{
\item{Y_t}{Matrix of observations, with one row per date. Missing entries are
allowed and are ignored in the updating step.}

\item{nu_t}{Matrix of transition intercepts, one row per date.}

\item{H}{State-transition matrix.}

\item{N}{Transition shock-loading matrix.}

\item{mu_t}{Matrix of measurement intercepts, one row per date.}

\item{G}{Measurement loading matrix.}

\item{M}{Measurement shock-loading matrix.}

\item{Sigma_0}{Initial covariance matrix of the latent state.}

\item{rho_0}{Initial mean of the latent state.}

\item{indic_pos}{Optional indicator vector. Components marked with \code{1} are
truncated below at zero after prediction and updating.}

\item{Rfunction}{Function returning the measurement-error covariance matrix.
By default, it is \code{function(M, ...) M \%*\% t(M)}.}

\item{Qfunction}{Function returning the transition-error covariance matrix.
By default, it is \code{function(N, ...) N \%*\% t(N)}.}

\item{reconciliationf}{Optional reconciliation function applied after the
update step, used for example in the quadratic Kalman filter.}
}
\value{
A list containing filtered states (\code{r}), filtered covariance matrices
(\code{Sigma_tt}), one-step-ahead predictions (\code{r_tp1_t}, \code{S_tp1_t}, \code{y_tp1_t},
\code{Omega_tp1_t}), fitted observables (\code{fitted.obs}), and the log-likelihood
contributions (\code{loglik}, \code{loglik.vector}).
}
\description{
Applies the Kalman filter to a linear Gaussian state-space system with
possibly time-varying intercepts and missing observations.
}
\details{
The measurement and transition equations are
\deqn{
y_t = \mu_t + G \rho_t + M \varepsilon_t,
}{
y_t = mu_t + G rho_t + M eps_t,
}
and
\deqn{
\rho_t = \nu_t + H \rho_{t-1} + N \xi_t.
}{
rho_t = nu_t + H rho_{t-1} + N xi_t.
}

The function automatically adapts the measurement update when some
observations are missing at a given date.

The default covariance functions are
\code{Rfunction(M, ...) = M \%*\% t(M)} and
\code{Qfunction(N, ...) = N \%*\% t(N)}, but custom functions can be supplied to
accommodate state-dependent volatility structures.
}
\examples{
# Example adapted from the filtering chapter of the Bookdown companion
# project. A one-dimensional latent AR(1) state drives two observables.
set.seed(123)
T <- 100
phi <- 0.9

Alpha <- matrix(c(0.8, 0.1,
                  0.2, 0.7), nrow = 2, byrow = TRUE)
Gamma <- matrix(c(1, 0.5), ncol = 1)
D <- diag(c(0.15, 0.15))

y <- matrix(0, nrow = 2, ncol = 1)
x <- 0
Y <- X <- NULL
Alpha_Y_1 <- NULL

for (t in 1:T) {
  Alpha_Y_1 <- rbind(Alpha_Y_1, c(Alpha \%*\% y))
  y <- Alpha \%*\% y + Gamma * x + D \%*\% rnorm(2)
  x <- phi * x + rnorm(1)
  Y <- rbind(Y, t(y))
  X <- rbind(X, x)
}

nu_t <- matrix(0, T, 1)
H <- matrix(phi, 1, 1)
N <- matrix(1, 1, 1)
mu_t <- Alpha_Y_1
G <- Gamma
M <- D
Sigma_0 <- matrix(1 / (1 - phi^2), 1, 1)
rho_0 <- 0

filter.res <- Kalman_filter(Y, nu_t, H, N, mu_t, G, M, Sigma_0, rho_0)
names(filter.res)
head(filter.res$r)

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
