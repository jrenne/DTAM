% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/credit-varg.R
\name{VARG_tools}
\alias{VARG_tools}
\alias{psi.VARG}
\alias{compute_proba_def_VARG}
\alias{compute_uncondmean_VARG}
\alias{compute_uncondvar_VARG}
\alias{simul_VARG}
\alias{make_VARG_Q}
\alias{prices_CDS_RFV_VARG}
\title{VARG transforms, moments, and simulation helpers}
\usage{
psi.VARG(u, psi.parameterization)

compute_proba_def_VARG(model, H = 10, indic_delta1 = NaN, X = NaN)

compute_uncondmean_VARG(model)

compute_uncondvar_VARG(model)

simul_VARG(model, nb_periods, w0 = NaN)

make_VARG_Q(model)

prices_CDS_RFV_VARG(model, H = 10, indic_delta1 = NaN, X = NaN)
}
\arguments{
\item{u}{Matrix of transform arguments.}

\item{psi.parameterization, model}{Lists of model parameters.}

\item{H}{Maximum horizon.}

\item{indic_delta1}{Optional index locating the first default-intensity
component within the state vector.}

\item{X}{Matrix of state-vector values where the pricing objects are
evaluated.}

\item{nb_periods}{Number of simulated dates.}

\item{w0}{Optional initial state.}
}
\value{
Depending on the function: affine-transform coefficients (\code{a}, \code{b}),
unconditional moments, simulated paths, transformed-measure parameters, or
pricing objects such as default probabilities and CDS spreads.
}
\description{
These functions cover the vector autoregressive gamma (VARG) model used in
the credit chapters. They provide the one-step Laplace transform under the
physical or transformed measure, unconditional moments, simulation, default
probabilities, and CDS pricing quantities.
}
\examples{
model <- list(
  alpha = c(0.5, 0.3),
  nu = c(1, 1.2),
  mu = c(0.2, 0.15),
  beta = matrix(c(0.4, 0.1,
                  0.0, 0.3), 2, 2, byrow = TRUE),
  alpha_w = c(0.1, 0.05),
  mu_R0 = 0,
  mu_R1 = matrix(0, 2, 1),
  xi0 = 0,
  xi1 = matrix(0, 2, 1)
)
res_psi <- psi.VARG(matrix(c(0.05, 0.02), 2, 1), model)
dim(res_psi$a)
compute_uncondmean_VARG(model)

}
