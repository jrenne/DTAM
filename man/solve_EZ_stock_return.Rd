% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recursive-preferences.R
\name{solve_EZ_stock_return}
\alias{solve_EZ_stock_return}
\title{Solve the affine approximation to stock returns under Epstein-Zin preferences}
\usage{
solve_EZ_stock_return(
  model,
  psi,
  Ew = NaN,
  z_s_bar_ini = 5,
  nb_loop_z_bar = 20,
  nb_loop_mu_z1 = 100,
  mu_s1_ini = NaN
)
}
\arguments{
\item{model}{A list containing the state-dynamics specification together with
the solved SDF quantities returned by \code{solve_EZ_SDF()} or
\code{solve_EZ_UnitEIS()}. It must include at least \code{mu_d0}, \code{mu_d1}, \code{eta0},
\code{eta1}, \code{alpha}, \code{Phi}, \code{Ew}, and \code{n_w}.}

\item{psi}{Conditional log-Laplace transform of the state vector. It must
accept arguments \verb{(u, model)} and return a list with components \code{a} and
\code{b}.}

\item{Ew}{Optional unconditional mean of the state vector. If left as \code{NaN},
the function uses \code{model$Ew}.}

\item{z_s_bar_ini}{Initial guess for the average log price-dividend ratio.}

\item{nb_loop_z_bar}{Number of outer fixed-point iterations.}

\item{nb_loop_mu_z1}{Number of inner fixed-point iterations.}

\item{mu_s1_ini}{Optional initial guess for the price-dividend loading vector.}
}
\value{
The input \code{model} list augmented with \code{mu_rs0}, \code{mu_rs1}, \code{mu_rs2},
\code{mu_s0}, \code{mu_s1}, \code{kappa0s}, \code{kappa1s}, and \code{z_s_bar}.
}
\description{
Computes the Campbell-Shiller style affine approximation to log stock
returns implied by a model already solved for its Epstein-Zin stochastic
discount factor.
}
\details{
The function takes as given the affine SDF coefficients and solves the
fixed-point system associated with the log-linearized price-dividend ratio.
The returned coefficients provide an affine approximation to stock returns in
the spirit of Bansal and Yaron (2004).
}
\examples{
# Example adapted from the recursive-preferences chapter of the companion
# Bookdown project.
gamma <- 10
delta <- 0.998
mu_c <- 0.0015
mu_d <- 0.0025
phi <- 0.979
beta <- 3
sigma <- 0.0078
varphi_e <- 0.044
varphi_d <- 4.5

model <- list(
  mu = matrix(c(0, mu_c, mu_d), 3, 1),
  Phi = matrix(c(phi, 1, beta,
                 0,   0, 0,
                 0,   0, 0), 3, 3, byrow = TRUE),
  Sigma = diag(sigma^2 * c(varphi_e, 1, varphi_d)^2),
  rho = 1 / 1.5,
  gamma = gamma,
  delta = delta,
  mu_c0 = 0,
  mu_c1 = matrix(c(0, 1, 0), 3, 1),
  mu_d0 = 0,
  mu_d1 = matrix(c(0, 1, 0.5), 3, 1),
  n_w = 3
)

model_solved <- solve_EZ_SDF(
  model,
  psi.GaussianVAR,
  z_bar_ini = 5,
  nb_loop_z_bar = 20,
  nb_loop_mu_z1 = 20
)

stock_solved <- solve_EZ_stock_return(
  model_solved,
  psi.GaussianVAR,
  z_s_bar_ini = 5,
  nb_loop_z_bar = 20,
  nb_loop_mu_z1 = 20
)

stock_solved$mu_rs0
stock_solved$mu_s1

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
