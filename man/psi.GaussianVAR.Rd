% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/procedures.R
\name{psi.GaussianVAR}
\alias{psi.GaussianVAR}
\title{One-step Laplace transform for a Gaussian VAR}
\usage{
psi.GaussianVAR(u, psi.parameterization)
}
\arguments{
\item{u}{Matrix of Laplace-transform loadings. Each column corresponds to one
transform evaluation.}

\item{psi.parameterization}{List containing \code{mu}, \code{Phi}, and \code{Sigma}.}
}
\value{
A list with two entries:
\code{a}, the coefficient multiplying the lagged state in the affine transform,
and \code{b}, the constant term.
}
\description{
Computes the one-period conditional Laplace transform associated with a
Gaussian VAR(1) state process.
}
\details{
The state dynamics are
\deqn{
w_t = \mu + \Phi w_{t-1} + \varepsilon_t,
\qquad \varepsilon_t \sim \mathcal{N}(0, \Sigma).
}{
w_t = mu + Phi w_{t-1} + eps_t, eps_t ~ N(0, Sigma).
}

If \code{u} has \code{k} columns, the function computes the \code{k} transforms in parallel.

This function is often used as the \code{psi} argument of \code{reverse.MHLT()},
\code{compute_AB_classical()}, and \code{compute_AB_thk()} in Gaussian affine models.
}
\examples{
model <- list(
  mu = matrix(c(0.01, 0.02), ncol = 1),
  Phi = matrix(c(0.95, 0.00,
                 0.05, 0.90), nrow = 2, byrow = TRUE),
  Sigma = diag(c(0.02, 0.01))
)

u <- matrix(c(1, 0,
              0, 1), nrow = 2)
psi.GaussianVAR(u, model)

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
