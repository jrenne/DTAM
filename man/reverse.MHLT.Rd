% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/procedures.R
\name{reverse.MHLT}
\alias{reverse.MHLT}
\title{Reverse multi-horizon Laplace transform recursion}
\usage{
reverse.MHLT(psi, u1, u2 = NaN, H, psi.parameterization)
}
\arguments{
\item{psi}{One-period conditional Laplace transform of the state process.}

\item{u1}{Loading matrix applied at the terminal horizon.}

\item{u2}{Loading matrix applied at intermediate horizons. If omitted, the
function uses \code{u2 = u1}.}

\item{H}{Maximum horizon, in model periods.}

\item{psi.parameterization}{List of parameters passed to \code{psi}.}
}
\value{
A list containing arrays \code{A}, \code{B}, \code{a}, and \code{b}. The arrays \code{A} and
\code{B} provide the affine coefficients of the multi-horizon Laplace transform,
while \code{a} and \code{b} store the same quantities divided by the horizon.
}
\description{
Computes the reverse-order multi-horizon Laplace transform recursion for a
Markov state process whose one-step conditional Laplace transform is known.
}
\details{
Specifically, the function evaluates objects of the form
\deqn{
\mathbb{E}_t \left[
  \exp\left(
    u_2^\prime w_{t+1} + \cdots + u_2^\prime w_{t+h-1} + u_1^\prime w_{t+h}
  \right)
\right].
}{
E_t[exp(u2' w_{t+1} + ... + u2' w_{t+h-1} + u1' w_{t+h})].
}

The function can process several loading vectors in parallel: if \code{u1} and
\code{u2} are \verb{n x k}, the recursion is computed simultaneously for the \code{k}
columns.

This recursion is a core building block for affine pricing formulas in the
package, including bond pricing and several recursive-utility applications.
}
\examples{
# Example adapted from the affine-process chapter of the companion Bookdown.
model <- list(
  mu = matrix(c(0.00, 0.02), ncol = 1),
  Phi = matrix(c(0.90, 0.00,
                 0.05, 0.85), nrow = 2, byrow = TRUE),
  Sigma = diag(c(0.01, 0.02))
)

U <- matrix(c(0, 1,
              -1, 1), nrow = 2)

AB <- reverse.MHLT(
  psi = psi.GaussianVAR,
  u1 = U,
  H = 5,
  psi.parameterization = model
)

dim(AB$A)
dim(AB$B)

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
