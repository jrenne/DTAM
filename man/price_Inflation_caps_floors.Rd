% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricing-options.R
\name{price_Inflation_caps_floors}
\alias{price_Inflation_caps_floors}
\title{Price inflation caps and floors}
\usage{
price_Inflation_caps_floors(
  W,
  H,
  ell = 0,
  all_K,
  Pi_t_minus_ell = NaN,
  psi,
  parameterization,
  max_x = 1e+05,
  dx_statio = 10,
  min_dx = 1e-05,
  nb_x1 = 5000
)
}
\arguments{
\item{W}{Matrix of state-vector values.}

\item{H}{Maximum maturity in model periods.}

\item{ell}{Indexation lag.}

\item{all_K}{Vector of strikes expressed at the model frequency.}

\item{Pi_t_minus_ell}{Optional lagged price index level.}

\item{psi}{One-step Laplace transform of the state vector.}

\item{parameterization}{List containing the model, short-rate parameters, and
inflation loadings.}

\item{max_x, dx_statio, min_dx, nb_x1}{Numerical integration settings.}
}
\value{
A list containing \code{Caplet}, \code{Floorlet}, \code{Caps}, \code{Floors}, and the
maturity grid used in the accumulation.
}
\description{
Prices inflation caplets/floorlets and the cumulative caps/floors built from
them.
}
\examples{
model <- list(n_w = 1, mu = matrix(0, 1, 1), Phi = matrix(0.9, 1, 1),
              Sigma12 = matrix(0.1, 1, 1))
W <- matrix(c(0, 0.1), 2, 1)
res <- price_Inflation_caps_floors(
  W = W, H = 2, all_K = c(0.01, 0.02),
  psi = psi.GaussianVAR,
  parameterization = list(
    model = model, xi0 = 0, xi1 = 0,
    mu_pi0 = 0.005, mu_pi1 = 0.1
  )
)
dim(res$Caps)

}
