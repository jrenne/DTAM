% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtering-kim94.R
\name{Kim94}
\alias{Kim94}
\alias{simul_SSRS}
\title{Markov-switching state-space simulation and Kim's filter}
\usage{
simul_SSRS(SSmodel, s0 = NaN, beta0, Z)

Kim94(SSmodel, Y.matrix, Z.matrix, beta0, P0, xi0 = NaN)
}
\arguments{
\item{SSmodel}{A list containing regime-specific arrays \code{H}, \code{A}, \code{R},
\code{mu}, \code{F}, \code{G}, and \code{Q}, plus the regime transition matrix \code{P}. The third
array dimension indexes regimes.}

\item{s0}{Optional initial regime for simulation. If omitted, the most likely
regime under the stationary distribution is used.}

\item{beta0}{Initial state mean.}

\item{Z, Z.matrix}{Matrix of exogenous variables, with one row per date.}

\item{Y.matrix}{Matrix of observations, with one row per date.}

\item{P0}{Initial state covariance matrix.}

\item{xi0}{Optional initial regime-probability vector. If omitted, the
stationary distribution of \code{SSmodel$P} is used.}
}
\value{
\code{simul_SSRS()} returns simulated regime indicators (\code{s.simul}),
observations (\code{y.simul}), and states (\code{beta.simul}). \code{Kim94()} returns the
sample log-likelihood (\code{loglik}), its date-specific contributions
(\code{loglik.vector}), filtered state means (\code{all.beta.tt}), filtered state
covariances in vectorized form (\code{all.P.tt}), and filtered regime
probabilities (\code{all.xi.t}).
}
\description{
\code{simul_SSRS()} simulates a linear Gaussian state-space model whose
coefficients depend on a finite-state Markov chain. \code{Kim94()} applies the
collapsing filter of Kim (1994), combining a Kalman update for every
transition between regimes with a Hamilton update of regime probabilities.
}
\details{
The model is
\deqn{y_t = H_{s_t}\beta_t + A_{s_t}Z_t + e_t,\quad
e_t \sim N(0,R_{s_t}),}
\deqn{\beta_t = \mu_{s_t} + F_{s_t}\beta_{t-1} + G_{s_t}v_t,\quad
v_t \sim N(0,Q_{s_t}),}
where \code{s_t} has transition matrix \code{P} (rows sum to one).
}
\examples{
# A scalar state with two regimes. Regime 2 has a higher state intercept.
model <- list(
  H = array(1, c(1, 1, 2)),
  A = array(0, c(1, 1, 2)),
  R = array(c(0.04, 0.04), c(1, 1, 2)),
  mu = array(c(-0.10, 0.10), c(1, 1, 2)),
  F = array(c(0.8, 0.8), c(1, 1, 2)),
  G = array(1, c(1, 1, 2)),
  Q = array(c(0.02, 0.02), c(1, 1, 2)),
  P = matrix(c(0.95, 0.05,
               0.10, 0.90), 2, 2, byrow = TRUE)
)
set.seed(123)
Z <- matrix(1, 40, 1)
sim <- simul_SSRS(model, beta0 = 0, Z = Z)
fit <- Kim94(model, sim$y.simul, Z, beta0 = 0, P0 = matrix(0.1))
head(fit$all.xi.t)
all(abs(rowSums(fit$all.xi.t) - 1) < 1e-10)

}
\references{
Kim, C.-J. (1994). Dynamic linear models with Markov-switching.
\emph{Journal of Econometrics}, 60, 1--22.

Kim, C.-J., and Nelson, C. R. (1999). \emph{State-Space Models with Regime
Switching}. MIT Press.
}
