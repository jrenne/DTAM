% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/credit-topdown.R
\name{TopDown_pricing}
\alias{TopDown_pricing}
\alias{compute_CDS_TopDown}
\alias{compute_G}
\alias{compute_tranche}
\title{Top-down credit pricing objects}
\usage{
compute_CDS_TopDown(model, H = 10, W, RR = 0.5, indic_Q = TRUE)

compute_G(
  model,
  W,
  gamma,
  v,
  b.matrix,
  H,
  indic_Q = TRUE,
  indic_upper = TRUE,
  max_x = 2000,
  dx_statio = 2,
  min_dx = 10^(-6),
  nb_x1 = 1000
)

compute_tranche(
  model,
  W,
  j,
  H,
  vector.of.a,
  RR = 0.5,
  max_x = 500,
  dx_statio = 0.1,
  min_dx = 1e-05,
  nb_x1 = 1000,
  indic_Q = TRUE
)
}
\arguments{
\item{model}{List of top-down model parameters.}

\item{H}{Maximum horizon.}

\item{W}{Matrix of state-vector values.}

\item{RR}{Recovery rate.}

\item{indic_Q}{Logical. If \code{TRUE}, work under the risk-neutral transform.}

\item{gamma, v}{Inputs defining the truncated payoff.}

\item{b.matrix}{Threshold matrix, one row per maturity.}

\item{indic_upper}{Logical determining whether the upper or lower transform is
computed.}

\item{max_x, dx_statio, min_dx, nb_x1}{Numerical integration settings.}

\item{j}{Segment index for tranche pricing.}

\item{vector.of.a}{Attachment/detachment points.}
}
\value{
\code{compute_CDS_TopDown()} returns a \verb{T x J x H} array of CDS spreads.
\code{compute_G()} returns the array of truncated-payoff values.
\code{compute_tranche()} returns a list with \code{tranche_prices}, \code{numerator}, and
\code{denominat}.
}
\description{
\code{compute_CDS_TopDown()} prices index-segment CDS contracts in the top-down
model. \code{compute_G()} evaluates the truncated-payoff building block used for
loss-function transforms. \code{compute_tranche()} combines these ingredients into
tranche prices.
}
\examples{
model <- list(
  alpha.n = c(0.2),
  beta.ny = matrix(0.1, 1, 1),
  beta.nn = matrix(0.3, 1, 1),
  beta.yy = matrix(0.2, 1, 1),
  beta.yn = matrix(0.05, 1, 1),
  alpha.y = c(0.1),
  nu.y = c(1),
  mu.y = c(0.4),
  alpha.m = matrix(0, 2, 1),
  xi0 = 0,
  xi1 = matrix(0, 2, 1),
  I = 1,
  n_w = 2
)
W <- matrix(c(0.3, 0), 1, 2)
cds <- compute_CDS_TopDown(model, H = 2, W = W)
dim(cds)

}
