% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaussian-models.R
\name{simul.GVAR}
\alias{simul.GVAR}
\title{Simulate a Gaussian VAR process}
\usage{
simul.GVAR(model, nb.sim, x0 = NaN, nb.replic = 1)
}
\arguments{
\item{model}{A list describing the VAR. It must contain \code{mu} and \code{Phi}, and
either \code{Sigma12} (the square-root innovation matrix) or \code{Sigma}
(the innovation covariance matrix).}

\item{nb.sim}{Number of simulated dates.}

\item{x0}{Initial state vector. If left at the default, the function starts
from the unconditional mean implied by \code{mu} and \code{Phi}.}

\item{nb.replic}{Number of independent simulated paths. When \code{nb.replic = 1},
the output is a matrix. For \code{nb.replic > 1}, the output is a three-way
array.}
}
\value{
If \code{nb.replic = 1}, a matrix of dimension \verb{nb.sim x n}, where \code{n} is
the dimension of the state vector. If \code{nb.replic > 1}, an array of
dimension \verb{nb.sim x n x nb.replic}.
}
\description{
Simulates trajectories from a Gaussian VAR(1) model of the form
\deqn{
x_t = \mu + \Phi x_{t-1} + \Sigma^{1/2}\varepsilon_t,
}{
x_t = mu + Phi x_{t-1} + Sigma^{1/2} eps_t,
}
where \eqn{\varepsilon_t \sim \mathcal{N}(0, I)}{eps_t ~ N(0, I)}.
}
\details{
If \code{model$Sigma12} is not provided, the function computes it as
\code{t(chol(model$Sigma))}.

This function is used repeatedly in the Bookdown companion project to
generate sample paths for affine Gaussian models before pricing or
estimation steps.
}
\examples{
# Example adapted from the Bookdown companion project.
set.seed(123)

model <- list(
  mu = matrix(c(0, 0), ncol = 1),
  Phi = matrix(c(0.95, 0.05,
                 0.00, 0.90), nrow = 2, byrow = TRUE),
  Sigma12 = diag(c(0.1, 0.2))
)

sim <- simul.GVAR(model, nb.sim = 50)
dim(sim)

sim_multi <- simul.GVAR(model, nb.sim = 20, nb.replic = 3)
dim(sim_multi)

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
