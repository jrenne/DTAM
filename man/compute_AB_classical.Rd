% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/affine-bond-pricing.R
\name{compute_AB_classical}
\alias{compute_AB_classical}
\title{Compute affine bond-pricing coefficients}
\usage{
compute_AB_classical(
  xi0,
  xi1,
  kappa0 = NaN,
  kappa1 = NaN,
  H,
  psi,
  psi.parameterization
)
}
\arguments{
\item{xi0}{Scalar intercept in the short-rate equation.}

\item{xi1}{Loading vector in the short-rate equation.}

\item{kappa0}{Optional intercept of the default intensity. If left as \code{NaN},
only risk-free bonds are priced.}

\item{kappa1}{Optional loading matrix for default intensities. Different
columns can correspond to different entities.}

\item{H}{Maximum maturity, expressed in model periods.}

\item{psi}{Conditional Laplace transform of the state vector.}

\item{psi.parameterization}{List of parameters passed to \code{psi}.}
}
\value{
A list with arrays \code{A}, \code{B}, \code{a}, and \code{b}.
}
\description{
Computes the affine coefficients used to price risk-free bonds and, when
default-intensity parameters are supplied, defaultable bonds.
}
\details{
Bond prices take the form
\deqn{
B_{t,h} = \exp\left(B_h + A_h^\prime w_t\right),
}{
B_{t,h} = exp(B_h + A_h' w_t),
}
and yields are
\deqn{
y_{t,h} = b_h + a_h^\prime w_t.
}{
y_{t,h} = b_h + a_h' w_t.
}
}
\examples{
model <- list(
  mu = matrix(c(0.01, 0.02), ncol = 1),
  Phi = matrix(c(0.9, 0.05,
                 0.00, 0.85), nrow = 2, byrow = TRUE),
  Sigma = diag(c(0.02, 0.01))
)
res <- compute_AB_classical(
  xi0 = 0.01,
  xi1 = matrix(c(0.1, 0.2), ncol = 1),
  H = 4,
  psi = psi.GaussianVAR,
  psi.parameterization = model
)
dim(res$A)

}
