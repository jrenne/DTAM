% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miscellaneous.R
\name{compute_Gaussian_fast}
\alias{compute_Gaussian_fast}
\title{Fast Gaussian multi-horizon aggregation}
\usage{
compute_Gaussian_fast(model, Maturities_decompo)
}
\arguments{
\item{model}{List containing \code{mu}, \code{Phi}, and \code{Sigma} for the Gaussian
VAR(1).}

\item{Maturities_decompo}{Integer decomposition matrix describing the target
maturities.}
}
\value{
A list with arrays \code{MU}, \code{PHI}, and \code{SIGMA}, plus a vector \code{H} of
maturities.
}
\description{
Computes the mean, autoregressive loading, and covariance associated with the
cumulated Gaussian VAR state \verb{x_\{t+1\} + ... + x_\{t+h\}} for a collection of
horizons.
}
\examples{
model <- list(
  mu = matrix(c(0, 0.1), 2, 1),
  Phi = matrix(c(0.8, 0.1,
                 0.0, 0.7), 2, 2, byrow = TRUE),
  Sigma = diag(c(0.2, 0.1))
)
decomp <- matrix(c(2, 0,
                   1, 2), 2, 2, byrow = TRUE)
res <- compute_Gaussian_fast(model, decomp)
res$H

}
