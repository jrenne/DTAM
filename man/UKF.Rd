% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtering-ukf.R
\name{UKF}
\alias{UKF}
\title{Unscented Kalman filter}
\usage{
UKF(
  Y,
  transition,
  measurement,
  Q,
  R,
  x0,
  P0,
  alpha = 0.001,
  beta = 2,
  kappa = 0
)
}
\arguments{
\item{Y}{Matrix of observations, with one row per date.}

\item{transition}{Function \code{transition(x, t)} returning the conditional mean
of the next state.}

\item{measurement}{Function \code{measurement(x, t)} returning the conditional
mean of the observation.}

\item{Q}{State-innovation covariance matrix.}

\item{R}{Measurement-innovation covariance matrix.}

\item{x0}{State mean immediately before the first transition.}

\item{P0}{Covariance matrix immediately before the first transition.}

\item{alpha, beta, kappa}{Parameters of the scaled unscented transform. For a
Gaussian prior, \code{beta = 2} is conventional.}
}
\value{
A list containing filtered and predicted state means
(\code{filtered.mean}, \code{predicted.mean}), the corresponding covariance arrays
(\code{filtered.cov}, \code{predicted.cov}), innovations and their covariance arrays
(\code{innovation}, \code{innovation.cov}), and the total and date-specific Gaussian
quasi-log-likelihood (\code{loglik}, \code{loglik.vector}).
}
\description{
Applies the scaled unscented Kalman filter to a nonlinear state-space model
with additive Gaussian transition and measurement noise:
\deqn{x_t = f(x_{t-1},t) + \varepsilon_t,\quad
\varepsilon_t \sim N(0,Q),}
\deqn{y_t = h(x_t,t) + \eta_t,\quad \eta_t \sim N(0,R).}
}
\examples{
# Nonlinear measurement equation: y_t = x_t + 0.1 x_t^2 + eta_t.
transition <- function(x, t) 0.8 * x
measurement <- function(x, t) x + 0.1 * x^2

set.seed(123)
TT <- 30
x <- numeric(TT)
y <- numeric(TT)
previous <- 0
for (t in seq_len(TT)) {
  x[t] <- transition(previous, t) + rnorm(1, sd = 0.2)
  y[t] <- measurement(x[t], t) + rnorm(1, sd = 0.1)
  previous <- x[t]
}

fit <- UKF(matrix(y), transition, measurement,
           Q = matrix(0.2^2), R = matrix(0.1^2),
           x0 = 0, P0 = matrix(0.2))
head(fit$filtered.mean)

}
