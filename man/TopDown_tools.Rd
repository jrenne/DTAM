% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/procedures.R
\name{TopDown_tools}
\alias{TopDown_tools}
\alias{psi.w.TopDown}
\alias{psiQ.w.TopDown}
\alias{psi.y.TopDown}
\alias{simul.TopDown}
\title{Top-down credit-model transforms and simulation}
\usage{
psi.w.TopDown(u, model, psi.y = psi.y.TopDown)

psiQ.w.TopDown(u, model, psi.y = psi.y.TopDown)

psi.y.TopDown(u.y, model)

simul.TopDown(model, nb_periods, W0 = NaN)
}
\arguments{
\item{u, u.y}{Matrix of transform arguments.}

\item{model}{List of top-down model parameters.}

\item{psi.y}{Optional function used to evaluate the transform of the \code{y}
block.}

\item{nb_periods}{Number of simulated dates.}

\item{W0}{Optional initial state.}
}
\value{
The \verb{psi.*} functions return affine-transform coefficients. The
simulator returns a list with unconditional moments (\code{EV}) and simulated
paths for \code{y}, \code{n}, and the stacked state \code{w}.
}
\description{
The \code{psi.*TopDown} functions evaluate the one-step Laplace transform of the
latent loss/intensity state used in the top-down credit model. \code{simul.TopDown}
simulates the state vector under the physical measure.
}
\examples{
model <- list(
  alpha.n = c(0.2),
  beta.ny = matrix(0.1, 1, 1),
  beta.nn = matrix(0.3, 1, 1),
  beta.yy = matrix(0.2, 1, 1),
  beta.yn = matrix(0.05, 1, 1),
  alpha.y = c(0.1),
  nu.y = c(1),
  mu.y = c(0.4),
  alpha.m = matrix(0, 2, 1),
  n_w = 2
)
psi.y.TopDown(matrix(0.05, 1, 1), model)$b
sim <- simul.TopDown(model, nb_periods = 10)
dim(sim$all_w)

}
