% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricing-options.R
\name{price_IR_caps_floors}
\alias{price_IR_caps_floors}
\title{Price interest-rate caps and floors}
\usage{
price_IR_caps_floors(
  W,
  H,
  tau,
  freq,
  all_K,
  psi,
  parameterization,
  max_x = 1e+05,
  dx_statio = 10,
  min_dx = 1e-05,
  nb_x1 = 5000
)
}
\arguments{
\item{W}{Matrix of state-vector values.}

\item{H}{Maximum maturity in model periods.}

\item{tau}{Maturity of the reference rate, in model periods.}

\item{freq}{Number of model periods per year.}

\item{all_K}{Vector of annualized strikes.}

\item{psi}{One-step Laplace transform of the state vector.}

\item{parameterization}{List containing the model and the short-rate
specification.}

\item{max_x, dx_statio, min_dx, nb_x1}{Numerical integration settings.}
}
\value{
A list containing \code{Caplet}, \code{Floorlet}, \code{Caps}, \code{Floors}, and the
affine coefficients of the reference rate.
}
\description{
Prices caplets/floorlets on an affine reference rate and accumulates them
into caps and floors.
}
\examples{
model <- list(
  n_w = 1,
  mu = matrix(0, 1, 1),
  Phi = matrix(0.9, 1, 1),
  Sigma12 = matrix(0.1, 1, 1)
)
W <- matrix(c(0, 0.1), 2, 1)
res <- price_IR_caps_floors(
  W = W, H = 4, tau = 1, freq = 4, all_K = c(0.01, 0.02),
  psi = psi.GaussianVAR,
  parameterization = list(model = model, xi0 = 0, xi1 = 0)
)
dim(res$Caps)

}
