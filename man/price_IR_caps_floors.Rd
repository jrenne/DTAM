% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/procedures.R
\name{price_IR_caps_floors}
\alias{price_IR_caps_floors}
\title{Price interest-rate caps and floors}
\usage{
price_IR_caps_floors(
  W,
  H,
  tau,
  freq,
  all_K,
  psi,
  parameterization,
  max_x = 1e+05,
  dx_statio = 10,
  min_dx = 1e-05,
  nb_x1 = 5000
)
}
\arguments{
\item{W}{Matrix of state values, with one row per date.}

\item{H}{Maximum maturity, in model periods.}

\item{tau}{Maturity of the reference rate, in model periods.}

\item{freq}{Number of model periods per year.}

\item{all_K}{Vector of annualized strike rates.}

\item{psi}{Conditional Laplace transform of the state vector.}

\item{parameterization}{List containing at least \code{model}, \code{xi0}, and \code{xi1}.
The embedded \code{model} object must include \code{n_w}.}

\item{max_x}{Maximum truncation point of the numerical integration grid.}

\item{dx_statio}{Constant step size used on the upper tail of the integration
grid.}

\item{min_dx}{Smallest step size used at the beginning of the grid.}

\item{nb_x1}{Number of log-spaced grid points used before switching to
\code{dx_statio}.}
}
\value{
A list containing:
\code{A_tau}, \code{B_tau}, \code{a_tau}, \code{b_tau} for the affine representation of the
reference rate, plus arrays \code{Caplet}, \code{Floorlet}, \code{Caps}, and \code{Floors}.
}
\description{
Prices caplets, floorlets, caps, and floors in an affine term-structure model
using Fourier inversion and affine pricing recursions.
}
\details{
The function first computes the affine representation of the reference rate,
then prices caplets and floorlets through repeated calls to
\code{truncated.payoff()}, and finally aggregates them into caps and floors across
maturities.

The entries of \code{all_K} are annualized strikes, whereas the underlying model
is expressed at the model frequency set by \code{freq}.
}
\examples{
model <- list(
  alpha = matrix(c(0.1, 0.2), ncol = 1),
  nu = matrix(c(0.2, 0.3), ncol = 1),
  mu = matrix(c(1, 1), ncol = 1),
  beta = matrix(c(0.9, 0,
                  0.1, 0.8), 2, 2, byrow = TRUE),
  n_w = 2
)

set.seed(123)
W <- simul_VARG(model, nb_periods = 30)
W <- t(W)

parameterization <- list(
  model = model,
  xi0 = 0.01 / 12,
  xi1 = matrix(c(0.5, 0.2), ncol = 1)
)

res <- price_IR_caps_floors(
  W = W,
  H = 12,
  tau = 3,
  freq = 12,
  all_K = c(0.01, 0.02),
  psi = psi.VARG,
  parameterization = parameterization,
  max_x = 500,
  dx_statio = 2,
  min_dx = 1e-4,
  nb_x1 = 300
)

names(res)
dim(res$Caps)

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
