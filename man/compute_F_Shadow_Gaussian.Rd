% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shadow_rate.R
\name{compute_F_Shadow_Gaussian}
\alias{compute_F_Shadow_Gaussian}
\title{Gaussian shadow-rate pricing recursion}
\usage{
compute_F_Shadow_Gaussian(
  W,
  psi.parameterization,
  ell_bar,
  b,
  a,
  c,
  H,
  indic_WuXia = TRUE
)
}
\arguments{
\item{W}{Matrix of state vectors, with one row per date.}

\item{psi.parameterization}{List containing \code{mu}, \code{Phi}, and \code{Sigma} for the
Gaussian state dynamics.}

\item{ell_bar}{Shadow-rate lower bound.}

\item{b, a, c}{Parameters entering the affine/lognormal approximation.}

\item{H}{Maximum horizon.}

\item{indic_WuXia}{Logical. If \code{TRUE}, use the Wu-Xia approximation.}
}
\value{
A list containing the pricing terms \code{F_c_equal_0}, \code{F}, and the
maturity-by-maturity coefficient sequences \code{all_b_bar_n}, \code{all_b_n}, and
\code{all_sigma_n}.
}
\description{
Computes the truncated-shadow-rate pricing objects used in the Gaussian
version of the shadow-rate model.
}
\examples{
model <- list(
  mu = matrix(0, 2, 1),
  Phi = diag(c(0.9, 0.8)),
  Sigma = diag(c(0.1, 0.2))
)
W <- matrix(c(0, 0,
              0.1, -0.1), 2, 2, byrow = TRUE)
res <- compute_F_Shadow_Gaussian(
  W = W,
  psi.parameterization = model,
  ell_bar = 0,
  b = 0,
  a = matrix(c(1, 0), 2, 1),
  c = matrix(c(0, 1), 2, 1),
  H = 3
)
dim(res$F)

}
