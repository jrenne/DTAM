% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recursive-preferences.R
\name{solve_CRRA}
\alias{solve_CRRA}
\title{Solve the CRRA stochastic discount factor}
\usage{
solve_CRRA(model, psi, Phi = NaN, du = 1e-06)
}
\arguments{
\item{model}{A list containing the preference and state-dynamics
specification. It must include at least \code{gamma}, \code{delta}, \code{mu_c0},
\code{mu_c1}, and \code{n_w}, together with the objects required by \code{psi}.}

\item{psi}{Conditional log-Laplace transform of the state vector. It must
accept arguments \verb{(u, model)} and return a list with components \code{a} and
\code{b}.}

\item{Phi}{Optional argument kept for interface consistency. If provided, it
is ignored because the function recomputes \code{Phi} through
\code{compute_expect_variance()}.}

\item{du}{Finite-difference step used to recover conditional moments of the
state process.}
}
\value{
The input \code{model} list augmented with \code{Ew}, \code{Vw}, \code{Phi}, \code{mu},
\code{eta0}, \code{eta1}, and \code{alpha}.
}
\description{
Solves the affine stochastic discount factor implied by standard
time-separable CRRA preferences.
}
\details{
Under CRRA time-separable preferences, the price of risk vector is simply
\code{-gamma * mu_c1}. The function computes the associated affine short-rate and
SDF coefficients after recovering the conditional moments of the state
process from \code{psi}.
}
\examples{
# Example adapted from the recursive-preferences chapter of the companion
# Bookdown project.
gamma <- 10
delta <- 0.998
mu_c <- 0.0015
mu_d <- 0.0025
phi <- 0.979
beta <- 3
sigma <- 0.0078
varphi_e <- 0.044
varphi_d <- 4.5

model <- list(
  mu = matrix(c(0, mu_c, mu_d), 3, 1),
  Phi = matrix(c(phi, 1, beta,
                 0,   0, 0,
                 0,   0, 0), 3, 3, byrow = TRUE),
  Sigma = diag(sigma^2 * c(varphi_e, 1, varphi_d)^2),
  gamma = gamma,
  delta = delta,
  mu_c0 = 0,
  mu_c1 = matrix(c(0, 1, 0), 3, 1),
  n_w = 3
)

res <- solve_CRRA(model, psi.GaussianVAR)

res$eta0
res$alpha

}
\references{
Monfort, A., Pegoraro, F., Renne, J.-P., and Roussellet, G. (2026).
\emph{Asset Pricing with Discrete-Time Affine Processes}.
}
