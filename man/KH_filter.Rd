% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtering-regime-switching.R
\name{KH_filter}
\alias{KH_filter}
\alias{KH_smoother}
\title{Filter and smooth finite-state regime probabilities}
\usage{
KH_filter(Omega, Eta)

KH_smoother(Omega, Eta)
}
\arguments{
\item{Omega}{Regime transition matrix with rows summing to one. It can also
be a \verb{J x J x T} array for deterministic time-varying transition
probabilities.}

\item{Eta}{Matrix of dimension \verb{T x J}. Entry \verb{(t, j)} is the log conditional
density (or log likelihood contribution) of observation \code{t} under regime
\code{j}.}
}
\value{
\code{KH_filter()} returns a list with:
\describe{
\item{\code{ksi_matrix}}{Filtered regime probabilities, one row per date.}
\item{\code{loglik_vec}}{Date-by-date log-likelihood contributions.}
\item{\code{loglik}}{Total log-likelihood.}
}
\code{KH_smoother()} returns a \verb{T x J} matrix of smoothed regime probabilities.
}
\description{
These functions implement the Kitagawa-Hamilton filter and smoother for a
hidden Markov chain. \code{KH_filter()} recursively updates filtered regime
probabilities and accumulates the log-likelihood contribution associated with
regime-specific conditional densities. \code{KH_smoother()} applies the backward
recursion that turns filtered probabilities into smoothed probabilities.
}
\details{
The implementation expects \code{Eta} on the log scale. Internally, it recenters
each row before exponentiating in order to reduce numerical underflow.
}
\examples{
Omega <- matrix(c(0.95, 0.05,
                  0.10, 0.90), 2, 2, byrow = TRUE)
Eta <- cbind(log(c(0.8, 0.2, 0.7)),
             log(c(0.2, 0.8, 0.3)))

filt <- KH_filter(Omega, Eta)
smth <- KH_smoother(Omega, Eta)
dim(filt$ksi_matrix)
dim(smth)

}
